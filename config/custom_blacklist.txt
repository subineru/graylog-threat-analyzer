# 自訂黑名單 — 已確認惡意 IP 或 CIDR
# 格式：單一 IP 位址或 CIDR 網段，每行一筆
# `#` 開頭為注解，空行忽略
# 此檔為範例，請依環境修改後使用
#
# 熱重載：POST /blacklist/reload

# 範例（取消注解後生效）：
# 203.0.113.0/24      # TEST-NET-3，文件用途
# 192.0.2.1           # TEST-NET-1，文件用途
